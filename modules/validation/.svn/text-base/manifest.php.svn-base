<?
{

  $module = new Module;

  /*
   * initialise the module.
   */
  $module->Define("validation",
		  "validation",
		  "This module provides the system with a validation system.");

  /*
   * import the module-specific objects.
   */
  $module->Import("objects/validation.php",
		  "Validation");

  /*
   * finally, register the module.
   */
  $context->module = $module;

}
?>
